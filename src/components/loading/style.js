import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    Loading: {
        display: 'flex',
        justifyContent: 'center',
    },
    Warning: {
        textAlign: 'center',
        marginVertical: 20,
    }
});